# Pages

This directory contains Astro route entrypoints for the homepage, publications
page, writing routes, custom content pages, and feed generation.

Ownership: template-owned. Downstream sites should create page content in
`src/content/pages/` and configure navigation through `src/site/config.ts`.

See [Repo Ownership](../../docs/repo-ownership.md) for the full policy.
